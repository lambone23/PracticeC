#include <iostream>

int main()
{


    // 분기문
    int dir = 1;
    if (dir == 1)
    {
        std::cout << "왼쪽으로 이동";
    }
    else if (dir == 2)
    {
        std::cout << "오른쪽으로 이동";
    }
    else if (dir == 3)
    {
        std::cout << "아래로 이동";
    }
    else if (dir == 4)
    {
        std::cout << "위로 이동";
    }
    else
    {
        std::cout << "STOP";
    }

    if (dir == 1)
    {
        std::cout << "왼쪽으로 이동";
    }
    if (dir == 2)
    {
        std::cout << "오른쪽으로 이동";
    }
    if (dir == 3)
    {
        std::cout << "아래로 이동";
    }
    if (dir == 4)
    {
        std::cout << "위로 이동";
    }

    // 시작

    // 전투

    // 종료


    return 0;
}

